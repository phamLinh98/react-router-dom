export const loadAction = {
    type: "load data from db",
    payload: null,
  };
  
  export const loadingAction = {
    type: "dang load data from db",
    payload: null,
  };
  
  export const loadedAction = {
    type: "da load data from db",
    payload: null,
  };
  
  export const loadError = {
    type: "load error data from db",
    payload: null,
  };
  